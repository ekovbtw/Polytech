Нужно поместить файл с выборкой из своего варианта .csv в папку с проектом notebook
или напрямую указать путь до файла. 
Как поместить файл: 
лучше создать папку с курсовой работой в самом jupyter найти ее и занести в нее новый notebook (как в прошлом пункте) и туда же upload выборку.
Можете отдельно установить pandas (библиотека для работы с таблицей) или в самом jupyter 
(для этого один раз в самом начане )
Код:
`import pandas as pd`
`data = pd.read_csv('var_9_dgamma.csv', header=None)`
`X = data.iloc[:, 0].values`
`print(len(X))`
