FILE* file = fopen("input_5.in", "r");
if (file == NULL)
{
    printf("No find file\n");
    return 1;
}
int matrix_file[58][58] = { 0 };
char buffer[1024]; // пропустили
fgets(buffer, sizeof(buffer), file); 
for (int i = 0; i < 58; i++)
{
    for (int j = 0; j < 58; j++)
    {
        fscanf(file, "%d", &matrix_file[i][j]);
    }
}

 После этого в array будут строки с элементами (доступные по индексу соответственно)
 