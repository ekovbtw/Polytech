Т.е. нам нужно создать список ребер, которые появляются с каждым шагом
Удобнее всего хранить их в матрице или в массиве (даже одномерном можно)
Я думаю буду хранить их в двумерном массиве
Попробую реализовать...
`int E[57][2] = { 0 }; // Множество ребер` объявлю глобальную матрицу

`void save_E(int index, int count) // функция для отслеживания ребер, которые добавляем (count -- количество добавлений)`
`{`
    `int old_index = from_row; // старый индекс`
    `int new_index = index; // новый`
    `if (old_index > new_index) // меняем (тк в задании написано в порядке возростания)`
    `{`
        `int t = old_index;`
        `old_index = new_index;`
        `new_index = t;`
    `}`
	`E[count][0] = old_index; // записываем`
    `E[count][1] = new_index;`
`}`
в main в цикл добавим:
`save_E(index,j);`
