После того как все потоки созданы и SetEvent вызван, main должен:

- Ждать сигнала done_event — это значит вся сортировка закончена
- Дождаться завершения всех потоков через WaitForMultipleObjects
- Записать результат в output.txt — количество потоков, N, и сам отсортированный массив
- Записать время в time.txt — но подумай, где именно нужно замерять старт и конец времени
- Вызвать close_all и закрыть все хэндлы потоков
