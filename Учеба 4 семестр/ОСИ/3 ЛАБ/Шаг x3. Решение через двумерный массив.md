Создать массив структур. В структуре - двоичный массив (комбинация) и булевая переменная проверки прошли комбинацию или нет.

#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>
#include <Windows.h>
#include <locale.h>
#include <stdbool.h>

#define LL long long
#define COMB_SIZE 100

typedef struct Combination
{
    int binary_sign[100];
    bool complete;
} Combination;

Combination global_task[COMB_SIZE];

void include_to_combination(int digit, int comb_index, int bit_index)
{
    global_task[comb_index].binary_sign[bit_index] = digit;
}

void init_combination(int count_An)
{
    int count_signs = count_An - 1;         // количество мест под +/-
	int count_combination = 1 << count_signs; // количество комбинаций равно 2^количества чисел

    printf("Количество комбинаций: %d\n", count_combination);

    // на всякий случай проверка
    if (count_combination > COMB_SIZE)
    {
        printf("Слишком много комбинаций для массива global_task\n");
        return;
    }

    // сначала очистим массив
    for (int i = 0; i < count_combination; i++)
    {
        for (int j = 0; j < count_signs; j++)
        {
            global_task[i].binary_sign[j] = 0;
        }
        global_task[i].complete = false;
    }

    // генерируем все комбинации
    for (int i = 0; i < count_combination; i++)
    {
        for (int j = 0; j < count_signs; j++)
        {
            int digit = (i >> j) & 1;

            include_to_combination(digit, i, count_signs - 1 - j);
        }
    }

    // отладка
    for (int i = 0; i < count_combination; i++)
    {
        for (int j = 0; j < count_signs; j++)
        {
            printf("%d ", global_task[i].binary_sign[j]);
        }
        printf("| complete = %d\n", global_task[i].complete);
    }
}

int main()
{
	setlocale(LC_ALL, "Russian");
    FILE* file = fopen("input.txt", "r");
    if (file == NULL)
    {
        printf("Не удалось открыть input.txt\n");
        return 1;
    }
    int count_threads = 0; // количество потоков
    int count_An = 0; // количество чисел
	fscanf(file, "%d", &count_threads);
    fscanf(file, "%d", &count_An);
	int* Array_An = (int*)malloc(count_An * sizeof(int)); // массив где хранятся числа
    for (int i = 0; i < count_An; i++)
    {
        fscanf(file, "%d", &Array_An[i]);
    }
    signed long long S;
    fscanf(file, "%lld", &S);

	init_combination(count_An);


    free(Array_An);
    fclose(file);
    return 0;
}
На выходе получим готовый массив структур

дальше нужно сделать функцию в которой будет алгоритм обращения к массиву структур и проверки не занятой комбинации. 

bool check_combination(int* Array_An, signed long long S, int count_An) // проверка комбинации на выполнение условия
{
    int count_signs = count_An - 1;
	int task_index = 0; //  ищем комбинацию, которая еще не была проверена
    for (int i = 0; i < COMB_SIZE; i++)
    {
		if (!global_task[i].complete)
        {
            task_index = i;
            break;
        }
        if (i == COMB_SIZE - 1)
        {
            printf("Все комбинации уже проверены\n");
            return false; // все комбинации уже проверены
		}
    }
	global_task[task_index].complete = true; // помечаем комбинацию как проверенную
    
	int* comb = global_task[task_index].binary_sign;
	int sum = Array_An[0]; // начинаем с первого числа
	for (int i = 0; i < count_signs; i++)
    {
        if (comb[i] == 1)
        {
			sum += Array_An[i + 1]; // ставим +
        }
        else
        {
           sum -= Array_An[i + 1]; // ставим -
        }
    }
	if (sum == S)
    {
        printf("Комбинация %d подходит\n", task_index);
        return true; // комбинация подходит
    }
    else
    {
        global_task[task_index].complete = true; // помечаем комбинацию как проверенную
        printf("Комбинация %d не подходит\n", task_index);
        return false; // комбинация не подходит
    }
       
        
    // считываем комбинацию из массива и пробуем расставить знаки +/-
    // если сумма совпала с S, то возвращаем true
	// иначе false
}

и в main: 
int count_combination = 1 << (count_An - 1);
int result = 0;
for (int i = 0; i < count_combination; i++)
{
    if (check_combination(Array_An, S, count_An))
    {
        printf("Подходит\n");
        result++;
    }
    else
    {
		printf("Не подходит\n");
    }
}
printf("\nRESULT = %d\n", result);

