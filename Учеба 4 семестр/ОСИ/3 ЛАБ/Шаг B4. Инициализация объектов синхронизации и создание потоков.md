В main, после считывания данных, нужно:

- Создать mutex, event и done_event с правильными параметрами — подумай какой тип сброса нужен для каждого события (автосброс или ручной), и в каком начальном состоянии они должны быть
- Положить первую задачу в очередь — весь массив от 0 до N-1
- Создать все рабочие потоки сразу — функцию потока пока можно оставить пустой заглушкой
- После создания потоков разбудить один из них — в очереди уже есть задачa


- done_event — ручной сброс. Когда работа завершена, нужно чтобы все потоки это увидели и никто не пропустил сигнал.
- Событие — автосброс. Потому что одна задача предназначена для одного потока. Разбудил одного — сигнал автоматически сбросился, остальные спят дальше.

В main: 

event = CreateEvent(NULL, FALSE, FALSE, NULL); // создание события для уведомления потоков о том, появилась задача
done_event = CreateEvent(NULL, TRUE, FALSE, NULL); // создание события для уведомления о завершении работы потоков
mutex = CreateMutex(NULL, FALSE, NULL); // создание мьютекса для синхронизации доступа к глобальному массиву

task_array[0].left = 0; // добавляем первую задачу 
task_array[0].right = N-1;
task_array[0].normal = true;
tail++;

HANDLE* threads = (HANDLE*)malloc(global_count_threads * sizeof(HANDLE)); // создаем потоки
if (threads == NULL)
{
    printf("Не удалось выделить память под потоки\n");
    fclose(file);
    close_all(threads);
    return 1;
}

for (int i = 0; i < global_count_threads; i++) // передаем их в функцию 
{
    threads[i] = CreateThread(NULL, 0, worker_thread, NULL, 0, NULL);
    if (threads[i] == NULL)
    {
        printf("Не удалось создать поток %d\n", i);
        fclose(file);
        for (int j = 0; j < i; j++)
        {
            WaitForSingleObject(threads[j], INFINITE);
            CloseHandle(threads[j]);
        }
            
        close_all(threads);
        return 1;
    }
}
SetEvent(event); // уведомление тк одна задача уже есть (стартовый массив)

Добавил: 

void close_all(HANDLE* threads) // шаг 4 добавил
{
    CloseHandle(mutex);
    CloseHandle(event);
    CloseHandle(done_event);
    free(global_array);
    free(task_array);
    free(threads);
}

и пока в качестве заглушки: 
DWORD WINAPI worker_thread(void* param) // шаг 4
{
    return 0;
}