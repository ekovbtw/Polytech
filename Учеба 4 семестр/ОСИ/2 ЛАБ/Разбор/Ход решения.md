Начнем с первой части tcpclient.cpp на Windows. По сути задания на вход подается файл с форматом: 
```
dd.mm.yyyy dd.mm.yyyy hh:mm:ss Message  
где  
dd.mm.yyyy - дата, например 11.09.2001  
hh:mm:ss - время, например: 23:14:51  
Message - текст неограниченной длины, до конца строки.  
Части записи отделяются друг от друга пробелом (одним).
```

## ШАГ 1
Для начала распарсим правильно файл: 

```
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>
#include <locale.h>
#include <string.h>
// 0 - ошибка в строке 
// 1 - строка корректна

FILE* file_open() // открываем файл
{
	FILE* file = fopen("file1.txt", "r");
	if (file == NULL)
	{
		printf("Error opening file!\n");
		exit(1);
	}
	return file;
}

int check_date(int day, int month, int year) // проверка корректности дат
{
	if (day < 1 || day > 31 || month < 1 || month > 12 || year < 0)
	{
		return 0;
	}
	if ((day == 31 && month == 4) || (day == 31 && month == 6) || (day == 31 && month == 9) || (day == 31 && month == 11))
	{
		return 0;
	}
	if (month == 2 && day > 29)
	{
		return 0;
	}
	return 1;
}

int check_time(int hour, int minute, int second) // проверка корректности времени
{
	if (hour < 0 || hour > 23 || minute < 0 || minute > 59 || second < 0 || second > 59)
	{
		return 0;
	}
	return 1;
}

int parse_file(char* buffer) // сам парсинг
{
	int day1, month1, year1;
	int day2, month2, year2;
	int hour, minute, second;
	char message[256];
	
	int point; // указатель для message (чтобы отслеживать на сколько переместились)
	if (sscanf(buffer, "%d.%d.%d %d.%d.%d %d:%d:%d %n", &day1, &month1, &year1, &day2, &month2, &year2, &hour, &minute, &second, &point) != 9) // сразу проверка на количество полученных значений 
	{
		return 0;
	}
	
	int status1 = check_date(day1, month1, year1); // проверяем даты
	int status2 = check_date(day2, month2, year2);
	if (status1 == 0 || status2 == 0)
	{
		return 0;
	}
	status1 = check_time(hour, minute, second); // проверяем время
	if (status1 == 0)
	{
		return 0;
	}
	/*printf("Date 1: %d.%d.%d\n", day1, month1, year1);
	printf("Date 2: %d.%d.%d\n", day2, month2, year2);
	printf("Time: %d:%d:%d\n", hour, minute, second);*/
	sscanf(buffer + point, "%[^\n]", message); // все символы кроме \n	(парсим message)
	printf("Message: %s\n", message);
	return 1;
}

int main()
{
	FILE* file = file_open(); 
	int counter = 0;
	char buffer[256]; // массив для строки
	while (fgets(buffer, sizeof(buffer), file) != NULL) // пока считывается строка 
	{
		if (strlen(buffer) == 1) // проверка на длину равную \n
		{
			continue;
		}
		int status = parse_file(buffer); // получаем успех/неудача
		if (status == 0) // проверка status
		{
			continue;
		}
		else
		{
			counter++;
		}
	}
	printf("%d\n", counter);
	fclose(file);
	return 0;
}
```

## ШАГ 2
Дальше сборка протокольного сообщения из распаршенных полей в набор байтов. 
Структура сообщения должна выглядеть так: 
- 4 байта — номер сообщения, 32-битное число в сетевом порядке байт, индексация от 0  
- 1 байт — день первой даты  
- 1 байт — месяц первой даты  
- 2 байта — год первой даты, unsigned short в сетевом порядке байт  
- 1 байт — день второй даты  
- 1 байт — месяц второй даты  
- 2 байта — год второй даты, тоже в сетевом порядке  
- 1 байт — час  
- 1 байт — минута  
- 1 байт — секунда  
- N байт — сам текст сообщения  
- 1 байт — нулевой байт-маркер конца сообщения

```
unsigned char* init_BUFFER(int len, int day1, int month1, int year1, int day2, int month2, int year2, int hour, int minute, int second, char* message, int number)
{
	unsigned char* BUFFER = NULL;
	BUFFER = (unsigned char*)malloc(sizeof(unsigned char)*(15 + len + 1)); // 15 байт на форматирование, len байт на сообщение, 1 байт на \0
	if (BUFFER == NULL)
	{
		exit(1);
	}
	int pointer = 0; // текущая позиция в буфере
	int buff = 0; // временная переменная 
	number_buff = htonl(number);  
	memcpy(BUFFER + pointer, &number_buff, sizeof(int)); 
	pointer += sizeof(int);	
	// преобразование в 1-2 байт (как по таблице)
	unsigned char day1_buf = (unsigned char)day1;
	unsigned char month1_buf = (unsigned char)month1;
	unsigned short year1_buf = (unsigned short)year1;
	unsigned char day2_buf = (unsigned char)day2;
	unsigned char month2_buf = (unsigned char)month2;
	unsigned short year2_buf = (unsigned short)year2;
	unsigned char hour_buf = (unsigned char)hour;
	unsigned char minute_buf = (unsigned char)minute;
	unsigned char second_buf = (unsigned char)second;

	//day1_buf = htons(day1_buf); // 1 байтные не стоит переводить, ломается нижний регистр
	//month1_buf = htons(month1_buf);
	year1_buf = htons(year1_buf);
	//day2_buf = htons(day2_buf);
	//month2_buf = htons(month2_buf);
	year2_buf = htons(year2_buf);
	//hour_buf = htons(hour_buf);
	//minute_buf = htons(minute_buf);
	//second_buf = htons(second_buf);
	memcpy(BUFFER+pointer, &day1_buf, 1); // копируем сдвигаем
	pointer += sizeof(unsigned char);
	memcpy(BUFFER+pointer, &month1_buf, 1);
	pointer += sizeof(unsigned char);
	memcpy(BUFFER+pointer, &year1_buf, sizeof(unsigned short));
	pointer += sizeof(unsigned short);
	memcpy(BUFFER+pointer, &day2_buf, 1);
	pointer += sizeof(unsigned char);
	memcpy(BUFFER+pointer, &month2_buf, 1);
	pointer += sizeof(unsigned char);
	memcpy(BUFFER+pointer, &year2_buf, sizeof(unsigned short));
	pointer += sizeof(unsigned short);
	memcpy(BUFFER+pointer, &hour_buf, 1);
	pointer += sizeof(unsigned char);
	memcpy(BUFFER+pointer, &minute_buf, 1);
	pointer += sizeof(unsigned char);
	memcpy(BUFFER+pointer, &second_buf, 1);
	pointer += sizeof(unsigned char);
	memcpy(BUFFER + pointer, message, len);
	pointer += len;
	memcpy(BUFFER + pointer, "\0", 1);
	pointer += 1;
	return BUFFER;
}
```

Также вызов объявим в parse_file: 

```
int parse_file(char* buffer, int number)
{
	int day1, month1, year1;
	int day2, month2, year2;
	int hour, minute, second;
	char message[256];
	
	int point;
	if (sscanf(buffer, "%d.%d.%d %d.%d.%d %d:%d:%d %n", &day1, &month1, &year1, &day2, &month2, &year2, &hour, &minute, &second, &point) != 9)
	{
		return 0;
	}
	
	int status1 = check_date(day1, month1, year1);
	int status2 = check_date(day2, month2, year2);
	if (status1 == 0 || status2 == 0)
	{
		return 0;
	}
	status1 = check_time(hour, minute, second);
	if (status1 == 0)
	{
		return 0;
	}
	/*printf("Date 1: %d.%d.%d\n", day1, month1, year1);
	printf("Date 2: %d.%d.%d\n", day2, month2, year2);
	printf("Time: %d:%d:%d\n", hour, minute, second);*/
	sscanf(buffer + point, "%[^\n]", message); // все символы кроме \n	
	//printf("Message: %s\n", message);
	int len = strlen(message);
	if (len > 255 || len == 0)
	{
		return 0;
	}
	unsigned char* BUFFER = init_BUFFER(len, day1, month1, year1, day2, month2, year2, hour, minute, second, message, number); // ТУТ

	free(BUFFER);
	return 1;
}
```

## ШАГ 3 
Третьим шагом будет разбор аргументов командой строки (той, что подается при запуске программы). 
Пример: **tcpclient 192.168.50.7:9000 file1.txt**
Где tcpclient - имя исполняемого бинарного файла клиента (получен путем компиляции tcpclient.cpp), 192.168.50.7 - IPv4 адрес сервера, 9000 - прослушиваемый сервером TCP-порт, file1.txt - файл с сообщениями.

```
bool check_args(int argc, char* argv[]) // проверка аргументов командной строки
{
	if (argc != 3)
	{
		printf("Uncorrect arguments %s\n", argv[0]);
		return false;
	}
	return true;
}

void parse_ip_and_port(char* ip_port_str, char* ip, int* port) // парсинг ip и порта из аргументов командной строки, используем указатели тк функция void
{
	// получаем ip и порт из строки ip_port_str, два массива ip и port передаем в функцию по указателю для сохранения значений
	if (sscanf(ip_port_str, "%15[^:]:%d", ip, port) != 2) // считываем ip и порт из строки
	{
		printf("Uncorrect arguments\n");
		exit(0);
	}
	int len = strlen(ip);
	if (len > 15 || len < 7) // проверка корректности ip
	{
		printf("Uncorrect arguments\n");
		exit(0);
	}
}

int main(int argc, char* argv[])
{
	setlocale(LC_ALL, "Russian");
	

	if (!check_args(argc, argv)) // проверка аргументов поданных на вход
	{
		return 1;
	}
	

	char ip[16]; // массив для хранения ip, 16 байт тк максимальная длина 15 символов + \0
	int port = 0; // переменная для хранения порта
	parse_ip_and_port(argv[1], ip, &port); // парсинг ip и порта из аргументов командной строки

	FILE* file = file_open(argv[2]);


	int counter = 0;
	char buffer[256];
	while (fgets(buffer, sizeof(buffer), file) != NULL)
	{
		if (strlen(buffer) == 1)
		{
			continue;
		}
		int status = parse_file(buffer, counter);
		if (status == 0)
		{
			continue;
		}
		else
		{
			counter++;
		}
	}
	printf("%d\n", counter);
	fclose(file);
	return 0;
}
```

## Шаг 4
В этом шаге нужно инициализировать сокет, создать сокет, если сокет не создан, то выписать ошибку, а также закрыть его. 

```
int init()
{
#ifdef _WIN32
	// Для Windows следует вызвать WSAStartup перед началом использования сокетов
	WSADATA wsa_data;
	return (0 == WSAStartup(MAKEWORD(2, 2), &wsa_data)); // (2.2) - версия Winsock, wsa_data - структура, которая будет заполнена информацией о версии 
#else
	return 1; // Для других ОС действий не требуется
#endif
}
void deinit()
{
#ifdef _WIN32
	// Для Windows следует вызвать WSACleanup в конце работы
	WSACleanup();
#else
	// Для других ОС действий не требуется
#endif
}

int sock_err(const char* function, int s) // функция для вывода ошибки сокета, принимает имя функции и сокет
{
	int err;
#ifdef _WIN32
	err = WSAGetLastError(); // возвращает код последней ошибки
#else
	err = errno;
#endif
	fprintf(stderr, "%s: socket error: %d\n", function, err); 
	return -1;
}

void s_close(int s) // функция для закрытия сокета, принимает сокет
{
#ifdef _WIN32
	closesocket(s);
#else
	close(s);
#endif
}

int main(int argc, char* argv[])
{
	setlocale(LC_ALL, "Russian");
	

	if (!check_args(argc, argv)) // проверка аргументов поданных на вход
	{
		return 1;
	}
	

	char ip[16]; // массив для хранения ip, 16 байт тк максимальная длина 15 символов + \0
	int port = 0; // переменная для хранения порта
	parse_ip_and_port(argv[1], ip, &port); // парсинг ip и порта из аргументов командной строки

	FILE* file = file_open(argv[2]); // закидываем имя файла

	int init_status = init(); // инициализация сокетов
	if (init_status == 0)
	{
		printf("Error initializing sockets\n");
		return 1;
	}


	int s;
	s = socket(AF_INET, SOCK_STREAM, 0); // создаем сокет, AF_INET - IPv4, SOCK_STREAM - TCP, 0 - протокол по умолчанию (TCP)
	if (s < 0)
	{
		return sock_err("socket", s);
	}

	int counter = 0;
	char buffer[256];
	while (fgets(buffer, sizeof(buffer), file) != NULL)
	{
		if (strlen(buffer) == 1)
		{
			continue;
		}
		int status = parse_file(buffer, counter);
		if (status == 1)
		{
			continue;
		}
		else
		{
			counter++;
		}
	}
	printf("%d\n", counter);
	fclose(file);
	deinit(); // деинициализация сокетов
	return 0;
}
```

## Шаг 5
На этом шаге нужно подключится к серверу (вместе с логикой повторов). Для начала посмотрим структуру для IPv4 адреса и порта 

```
typedef struct sockaddr_in // структура для хранения ipv4 адреса и порта
{
	short sin_family; //семейство протоколов: AF_INET
	unsigned short sin_port; //номер порта
	struct in_addr sin_addr; //IP-адрес
	char sin_zero[8]; //не используется, заполняется нулями
}sockaddr_in;
```
Её объявлять не нужно, тк в библиотеке winsock2.h она уже определена. 
Теперь перед заполнением нужно обнулить структуру. В main() добавим следующее, после создания сокета: 

```
/* обнуление и заполнение структуры адреса */
memset(&addr, 0, sizeof(addr));
addr.sin_family = AF_INET; // IPv4
addr.sin_port = htons(port); // преобразуем порт в сетевой порядок байтов
int status_inet_pton = inet_pton(AF_INET, ip, &addr.sin_addr); // преобразуем ip из строки в структуру in_addr
if (status_inet_pton == -1 || status_inet_pton == 0) // -1 если некорректный адрес, 0 если адрес по длине корректный, но диапазон чисел некорректный
{
	printf("Error inet_pton\n");
	return 1; 
}
```

Теперь немного изменим создание сокета. Нам нужно, чтобы connect был до того момента, пока не подключение не будет установлено или количество connect превысит 10. Причем между каждым connect должен быть промежуток 100мс. Напишем цикл:

```
// Установка соединения с удаленным хостом
int s; // сокет
int counter_connect = 0; // счетчик до 10 попыток
int connect_status = -1; // -1 если неудача, 0 если удача
while (counter_connect < 10)
{
	s = socket(AF_INET, SOCK_STREAM, 0); // один сокет = одна итерация, иначе будет работать не корректно
	if (s < 0)
	{
		return sock_err("socket", s);
	}
	if (connect(s, (struct sockaddr*)&addr, sizeof(addr)) == 0) // если подключение успешно
	{
		connect_status = 0; // удача
		break;
	}
	// иначе
	counter_connect++;
	s_close(s); // закрытие сокета, чтобы его пересоздать
	Sleep(100);	// спим 100 мс
}
if (connect_status == -1) // если так и не подключился
{
	s_close(s);
	return sock_err("connect", s);
}
```

Я перенес создание сокета в цикл, потому что при каждом отдельном подключении, сокет должен быть разным. После первого прохода сокет запоминает свое состояние неудачи и просто начнет его дублировать. 

## Шаг 6
В этом шаге нужно написать функцию отправки сообщения на сервер. Для начала это будет сообщение put, 3 байтное. 

```
// Отправляет put 
int send_put(int s)
{
	char buffer[3];
	buffer[0] = 'p';
	buffer[1] = 'u';
	buffer[2] = 't';

	int size = sizeof(buffer); // 3 байта
	int sent = 0; // количество отправленных 
#ifdef _WIN32
	int flags = 0;
#else
	int flags = MSG_NOSIGNAL;
#endif
	while (sent < size) 
	{
		// Отправка очередного блока данных
		int res = send(s, buffer + sent, size - sent, flags);
		if (res < 0)
			return sock_err("send", s);
		sent += res;
		printf(" %d bytes sent.\n", sent);
	}
	return 0;
}
```

## Шаг 7
Теперь по аналогии с пунктом 6, нужно написать функцию для отправки текста BUFFER. 

```
// отправка сообщения (по аналогии выше)
int send_BUFFER(int s, unsigned char* buffer, int len)
{
	int sent = 0;
#ifdef _WIN32
	int flags = 0;
#else
	int flags = MSG_NOSIGNAL;
#endif
	while (sent < len)
	{
		// Отправка очередного блока данных
		int res = send(s, (char*)buffer + sent, len - sent, flags);
		if (res < 0)
			return sock_err("send", s);
		sent += res;
		printf(" %d bytes sent.\n", sent);
	}
	return 0;
}
```

Объявим вызовы функций: в main() после connect: 

```
int status_send = send_put(s);
if (status_send != 0)
{
	printf("Error send put\n");
	return 1;
}
```

А также изменим конец в parse_file, объявим там вызов send_BUFFER: 

```
int parse_file(char* buffer, int number, int s) // парсинг строки из файла
{
	// день, месяц, год, день, месяц, год, час, минута, секунда, сообщение
	int day1, month1, year1;
	int day2, month2, year2;
	int hour, minute, second;
	char message[256];
	
	int point; // позиция в строке после sscanf (для дальнейшего считывания сообщения)
	if (sscanf(buffer, "%d.%d.%d %d.%d.%d %d:%d:%d %n", &day1, &month1, &year1, &day2, &month2, &year2, &hour, &minute, &second, &point) != 9) // считываем данные из строки
	{
		return 1;
	}
	
	int status1 = check_date(day1, month1, year1); // проверяем корректность даты
	int status2 = check_date(day2, month2, year2);
	if (status1 == 0 || status2 == 0) 
	{
		return 1;
	}
	int status3 = check_time(hour, minute, second); // проверяем корректность времени
	if (status3 == 0)
	{
		return 1;
	}
	
	// сканируем сообщение из строки, начиная с позиции point 
	if (sscanf(buffer + point, "%[^\n]", message) != 1) // все символы кроме \n	
	{
		return 1;
	}
	//printf("Message: %s\n", message);

	int len = strlen(message); // проверка корректности message
	if (len > 255 || len == 0)
	{
		return 1;
	}

	unsigned char* BUFFER = init_BUFFER(len, day1, month1, year1, day2, month2, year2, hour, minute, second, message, number);
	int len_BUFFER = len + 15 + 1; 
	int status_send = send_BUFFER(s, BUFFER, len_BUFFER);
	if (status_send != 0)
	{
		printf("Error send BUFFER\n");
		return 1;
	}
	free(BUFFER);
	return 0;
}
```

