	mov bx, msg

`print_first_str:`
		`mov al, [bx]`
		`test al, al`
		`jz done`
		`int 0x10`
		`inc bx`
		`jmp print_first_str`

`done:`
	`jmp $`

`turn_symbol_array: times 26 db 0`
`msg db 'Choose and turns symbols...',0`

Идея такая:

1. В `bx` кладёшь адрес строки
2. Берёшь байт из `[bx]`
3. Если это `0` — строка закончилась
4. Иначе печатаешь символ через `int 0x10`
5. Увеличиваешь `bx`
6. Повторяешь