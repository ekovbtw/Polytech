`mov ah, 0x0E`

    mov al, 'a'
    int 0x10
    mov al, 'b'
        int 0x10
        mov al, 'c'
        int 0x10
        mov al, 'd'
        int 0x10
        mov al, 'e'
        int 0x10
        mov al, 'f'
        int 0x10
        mov al, 'g'
        int 0x10
        mov al, 'h'
        int 0x10
        mov al, 'i'
        int 0x10
        mov al, 'j'
        int 0x10
        mov al, 'k'
        int 0x10
        mov al, 'l'
        int 0x10
        mov al, 'm'
        int 0x10
        mov al, 'n'
        int 0x10
        mov al, 'o'
        int 0x10
        mov al, 'p'
        int 0x10
        mov al, 'q'
        int 0x10
        mov al, 'r'
        int 0x10
        mov al, 's'
        int 0x10
