`void add_to_matrix(int matrix_file[58][58], int index) // добавление в матрицу`
`{`
    `if (array_turn_index[index] == 1) // проверка (на всякий) если уже есть вершина`
    `{`
        `printf("Error vershina uzhe 1");`
        `return;`
    `}`
    `array_turn_index[index] = 1; // иначе добавляем вершину`
    `for (int i = 0; i < 58; i++)`
    `{`
        `matrix_1[index][i] = matrix_file[index][i]; // копируем веса (новой вершины) в матрицу`
    `}`
`}`

В MAIN: 
`for (int j = 0; j < 57; j++) // 57 раз т.к. первый раз делали только для 17 вершины` 
`{`
    `int index = find_next_row(); // получаем индекс`
    `if (index == -1) // проверка на всякий`
    `{`
        `printf("Error: next vertex not found\n");`
        `break;`
    `}`
    `add_to_matrix(matrix_file, index);// добавляем`
`}`