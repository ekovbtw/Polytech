**Матрица D** - матрица, в которой храниться расстояние (минимальное) от вершины A[i] до A[k]. 
Изначально в матрице D - исходная матрица (как в варианте)
Затем в ней появляются обновления по мере прохождения алгоритма Флойда. (мы задали ее в прошлом пункте)
**Матрица P** - матрица, в которой хранятся второстепенные вершины. Т.е. вершины, из которых можно добраться из A[i] в A[k]. Например через A[j]. 
![[Pasted image 20260413211205.png]]
Инициализация матриц
![[Pasted image 20260413211424.png]]

Т.к. матрицу D мы инициализировали в прошлом пункте, то сейчас попробуем сделать правильную матрицу P 
Инициализация матрицы P
![[Pasted image 20260413213252.png]]
Инициализация функций
![[Pasted image 20260413213308.png]]
Обновленные принты
![[Pasted image 20260413213336.png]]

### Полный код: 

`#define _CRT_SECURE_NO_WARNINGS`
`#include <stdio.h>`
`#include <stdlib.h>`
`#include <string.h>`
`int kol_vershin = 5; // переменная для хранения количества вершин в графе`

`int matrix_D[5][5] = { 0 }; // матрица для хранения расстояний`
`int matrix_P[5][5] = { 0 }; // матрица для хранения промежуточных вершин`

`void print_matrix_D(); // отладочная функция для печати матрицы D на экран`
`void print_matrix_P(); // отладочная функция для печати матрицы P на экран`

`void init_P_matrix() // функция для инициализации матрицы P`
`{`
    `for (int i = 0; i < kol_vershin; i++)`
    `{`
        `for (int j = 0; j < kol_vershin; j++)`
        `{`
            `matrix_P[i][j] = i+1;` 
        `}`
    `}`
`}`

`int main()`
`{`
	`FILE* file = fopen("Floid.in", "r");` 
    `if (file == NULL)`
    `{`
        `printf("No find file\n");`
        `return 1;`
    `}`
	`char buffer[100]; // буфер для чтения строк из файла`
    `char skip[1024]; // пропустили`
    `fgets(skip, sizeof(skip), file);`
	`for (int i = 0; i < kol_vershin; i++) // цикл для чтения данных из файла и заполнения матрицы`
    `{`
        `for(int j = 0; j < kol_vershin; j++)`
        `{`
            `fscanf(file, "%s", buffer); // считываем данные из файла и сохраняем в матрицу`
            `if (strcmp(buffer, "*") == 0)`
            `{`
                `matrix_D[i][j] = 999999; // если символ "*", то сохраняем 999999 в матрице`
            `}`
            `else`
            `{`
                `matrix_D[i][j] = atoi(buffer); // иначе сохраняем число, преобразованное из строки`
            `}`
        `}`
    `}`
	`print_matrix_D();`
	`init_P_matrix(); // инициализируем матрицу P`
    `print_matrix_P(); // печатаем матрицу P для проверки`
    `fclose(file);`
    `return 0;`
`}`

`void print_matrix_D()` 
`{`
    `printf("D\n");`
    `for (int i = 0; i < kol_vershin; i++)`
    `{`
        `for (int j = 0; j < kol_vershin; j++)`
        `{`
            `printf("%d ", matrix_D[i][j]);`
        `}`
        `printf("\n");`
    `}`
`}`

`void print_matrix_P()` 
`{`
    `printf("\nP\n");`
    `for (int i = 0; i < kol_vershin; i++)`
    `{`
        `for (int j = 0; j < kol_vershin; j++)`
        `{`
            `printf("%d ", matrix_P[i][j]);`
        `}`
        `printf("\n");`
    `}`
`}`