нужно учесть что есть символ Ñ
это можно вывести если написать \xa4 
пример: 
str_str("ni\xa4o");  
back_n();
думаю сделать словарь вида : (ni~o) где ~ это Ñ
но добавить проверку в str_str если встретили ~ .
добавим 

void str_str(const char* point)
{
    volatile char* video = (volatile char*)0xB8000;

    while (*point)
    {
    	unsigned char ch; // тут

        if (*point == '~')
        {
            ch = 0xA4;   // пример, проверить в QEMU
        }
        else
        {
            ch = (unsigned char)(*point);
        }
        video[position * 2] = *point;
        video[position * 2 + 1] = 0x07;
        position++;
        point++;
    }

    update_cursor();
}


