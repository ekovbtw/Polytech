Сделать:
- IDT, **таблица IDT** — список, где для каждого номера прерывания записан адрес обработчика; 
- **общий запуск IDT** — загрузить её в процессор через `lidt`;
- обработчик клавиатуры - функция, которая будет вызываться при нажатии клавиши;
- включение прерываний. [[Прерывания]]. Команда `sti`.
- Общие требования к ядру: **настроить прерывания и включить их**. Иначе интерактивный ввод команд не заработает нормально.
- 