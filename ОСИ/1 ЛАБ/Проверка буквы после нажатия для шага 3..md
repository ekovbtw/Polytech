Проверим, что нажата буква с помощью
`cmp al, 'a'`  
`jb not_letter`  
  
`cmp al, 'z'`  
`ja not_letter`
т.е jb - меньше (если нажатый символ меньше индексом чем "а", то переход на not_letter (там будет прописан код для такого случая), ja - больше (аналогично))


Также на этом этапе были переработаны некоторые функции, вот изменения:


	mov bx, msg 
	call print_str  // вызывается функция через call для того чтобы использовать ret, чтобы код нормально завершался
			
	mov ah, 0x00 
	int 0x16
	cmp al, 'a'
	jb not_a_symbol
	cmp al, 'z'
	ja not_a_symbol
	
	jmp done

`print_str:`
		`mov al, [bx]`
		`test al, al`
		`jz print_str_end`
		`int 0x10`
		`inc bx`
		`jmp print_str`
		
`print_str_end:`
	`ret`

`not_a_symbol:` 
	`mov bx, msg_not_a_symbol`
	`mov ah, 0x0E`
	`call print_str`
	`jmp done`

`done:`
	`jmp $`

`turn_symbol_array: times 26 db 0`
`msg db 'Choose and turns symbols...',0`
`msg_not_a_symbol db 'DONT SYMBOL, Please choose symbol again',0`


PS
turn_show_str:
	mov ah, 0x0E
добавлена функция переключения на вывод текста (чтобы постоянно не писать, она потом проваливается в print_str_loop (в прошлую функцию print_str))